# Generated binaries and corresponding assets
cmd/ubuntu-report/ubuntu-report
*/*/build
libsysmetrics.so.*

# Binaries for programs and plugins
*.so

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool
*.out
